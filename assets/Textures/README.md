# RTOneWeekend/assets/Textures

This directory is for larger/unnecessary textures that aren't saved in the repository. It is left in for a convenient folder to easily add textures to.

For included textures (ones that went with the tutorial with maybe a couple of extra options), go to the **RTOneWeekend/assets/SampleTextures/** directory.